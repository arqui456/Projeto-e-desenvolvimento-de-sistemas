version: '3'
services:
  api:
    image: node-docker-backend
    build: .
    networks:
      - backend
    ports:
      - "3000:3000"

  web-cli:
    image: node-docker-backend
    networks:
      - backend
    command: sh

networks:
  backend:
    driver: bridge
    
